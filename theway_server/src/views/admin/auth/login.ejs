<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>관리자 로그인 - Way3 Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            background: radial-gradient(circle at top left, rgba(13,110,253,0.25), transparent 45%),
                        radial-gradient(circle at bottom right, rgba(111,66,193,0.25), transparent 40%),
                        #0b1526;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            color: #f8f9fa;
        }
        .login-wrapper {
            width: 100%;
            max-width: 420px;
            padding: 2.5rem 2rem;
            background: rgba(15, 23, 42, 0.82);
            border-radius: 16px;
            box-shadow: 0 18px 45px rgba(0,0,0,0.35);
            backdrop-filter: blur(18px);
            position: relative;
            overflow: hidden;
        }
        .login-wrapper::before {
            content: '';
            position: absolute;
            top: -40px;
            right: -40px;
            width: 140px;
            height: 140px;
            background: rgba(13,110,253,0.35);
            filter: blur(40px);
            z-index: 0;
        }
        .brand {
            position: relative;
            z-index: 1;
            text-align: center;
            margin-bottom: 2rem;
        }
        .brand-logo {
            width: 56px;
            height: 56px;
            border-radius: 14px;
            background: linear-gradient(135deg, #0d6efd, #6f42c1);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 0.9rem;
            color: #fff;
            font-size: 1.75rem;
            box-shadow: 0 10px 25px rgba(13,110,253,0.25);
        }
        .form-label {
            font-weight: 600;
            color: #cbd5f5;
        }
        .form-control {
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.1);
            color: #f8f9fa;
            padding: 0.75rem 1rem;
        }
        .form-control:focus {
            background: rgba(13,110,253,0.12);
            border-color: rgba(13,110,253,0.65);
            box-shadow: 0 0 0 0.15rem rgba(13,110,253,0.35);
            color: #fff;
        }
        .btn-login {
            background: linear-gradient(135deg, #0d6efd, #6f42c1);
            border: none;
            padding: 0.85rem;
            font-weight: 600;
            letter-spacing: 0.03em;
            box-shadow: 0 12px 30px rgba(13,110,253,0.3);
        }
        .btn-login:hover {
            background: linear-gradient(135deg, #0b5ed7, #5a32a3);
        }
        .meta-info {
            margin-top: 1.5rem;
            font-size: 0.85rem;
            color: #94a3c7;
            text-align: center;
        }
        .alert-custom {
            background: rgba(220,53,69,0.15);
            border: 1px solid rgba(220,53,69,0.35);
            color: #ffb3bd;
        }
        .form-text {
            color: #90a4d4;
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="brand">
            <div class="brand-logo">
                <i class="bi bi-graph-up"></i>
            </div>
            <h2 class="fw-bold mb-1">Way3 Admin</h2>
            <p class="text-muted mb-0">네오-서울 트레이딩 운영센터</p>
        </div>

        <% if (typeof error !== 'undefined') { %>
        <div class="alert alert-custom d-flex align-items-center" role="alert">
            <i class="bi bi-exclamation-triangle me-2"></i>
            <div><%= error %></div>
        </div>
        <% } %>

        <form method="POST" action="/admin/login" class="position-relative" autocomplete="off">
            <div class="mb-3">
                <label for="username" class="form-label">관리자 아이디</label>
                <div class="input-group">
                    <span class="input-group-text bg-transparent text-muted">
                        <i class="bi bi-person-fill"></i>
                    </span>
                    <input type="text" class="form-control" id="username" name="username" placeholder="admin" required autofocus>
                </div>
            </div>

            <div class="mb-2">
                <label for="password" class="form-label">비밀번호</label>
                <div class="input-group" id="passwordWrapper">
                    <span class="input-group-text bg-transparent text-muted">
                        <i class="bi bi-shield-lock"></i>
                    </span>
                    <input type="password" class="form-control" id="password" name="password" placeholder="비밀번호를 입력하세요" required>
                    <button class="btn btn-outline-light border-0" type="button" id="togglePassword" aria-label="비밀번호 표시">
                        <i class="bi bi-eye"></i>
                    </button>
                </div>
                <div class="form-text">기본 인증 정보: <code>admin / admin123</code></div>
            </div>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <div class="form-check form-switch text-light">
                    <input class="form-check-input" type="checkbox" id="rememberMe" name="remember">
                    <label class="form-check-label" for="rememberMe">자동 로그인</label>
                </div>
                <a href="#" class="link-light text-decoration-none small opacity-75" onclick="alert('비밀번호 재설정 기능은 준비 중입니다.'); return false;">
                    비밀번호를 잊으셨나요?
                </a>
            </div>

            <button type="submit" class="btn btn-login w-100 text-white">
                <i class="bi bi-box-arrow-in-right me-1"></i>
                대시보드 진입
            </button>
        </form>

        <div class="meta-info">
            <div class="d-flex justify-content-center gap-2">
                <span><i class="bi bi-shield-lock"></i> 안전한 접속</span>
                <span>•</span>
                <span><i class="bi bi-clock-history"></i> 24시간 관제</span>
            </div>
            <div class="mt-2 text-uppercase" style="letter-spacing: 0.2em; font-size: 0.7rem;">
                NEO-SEOUL OPERATIONS CENTER
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const togglePassword = document.getElementById('togglePassword');
        const passwordInput = document.getElementById('password');
        togglePassword.addEventListener('click', () => {
            const isHidden = passwordInput.getAttribute('type') === 'password';
            passwordInput.setAttribute('type', isHidden ? 'text' : 'password');
            togglePassword.querySelector('i').classList.toggle('bi-eye');
            togglePassword.querySelector('i').classList.toggle('bi-eye-slash');
        });
    </script>
</body>
</html>
